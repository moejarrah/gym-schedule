# Design References

These files are non-production design material.

- `ui-concepts/` contains standalone interface concepts.
- `generated-art/` contains generated visual references.

Nothing in this directory is loaded by the deployed PWA.
