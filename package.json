{
  "name": "gym-schedule",
  "private": true,
  "type": "module",
  "scripts": {
    "preview": "python3 -m http.server 4173",
    "check": "node --check app.js && node --check data.js && node --check storage.js && node --check sw.js && node --test tests/*.test.mjs"
  }
}
